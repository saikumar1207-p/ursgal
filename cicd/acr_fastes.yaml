resources:
  pipelines:
    - pipeline: mainpipe
      source: mainpipeline1
      trigger: true

trigger:
  branches:
    include:
      - main
      - dev
      - feature/*

pool:
  name: 'default'

steps:
- script: echo "Pipeline B triggered after Pipeline A finished"
