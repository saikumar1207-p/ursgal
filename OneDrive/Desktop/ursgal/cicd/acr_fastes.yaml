
resources:
  pipelines:
    - pipeline: mainpipe
      source: mainpipeline
      trigger: true

trigger: none

pool:
  vmImage: 'windows-latest'
  name: 'default'

steps:
- script: echo "Pipeline B triggered after Pipeline A finished"